import {} from '../utils/getLastSunday';

export class CoreParser extends DOMParser {
  //  CoreParser is the base class that the page-specific parsers are derived from.
  //  It can request 
  getHTML(query_param) {}
}
